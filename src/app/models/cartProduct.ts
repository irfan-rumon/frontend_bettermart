export interface CartProduct {
    productID: string;
    userID: string;
    imageURL: string;
    name: string;
    unitPrice : number;
    quantity: number;
    brand: string;
    subtotal: number;
}